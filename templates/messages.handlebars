/* eslint-disable */
/* tslint:disable */

import { BehaviorSubject } from 'rxjs'
import { InjectionToken, Provider } from '@angular/core';
import { {{metaClassName}} } from './{{metaFilename}}';

export const {{className}}InjectionToken = new InjectionToken<{{className}}>('{{className}}InjectionToken');
export const {{className}}Provider: Provider = {
    provide: {{className}}InjectionToken,
    useFactory: () => {{metaClassName}}.get()
};

/**
 * Translation messages interface
 */
{{>interface}}